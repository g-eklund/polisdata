Sys.setenv(LANG = "en")

event_day <-28
event_weekday <- "lördag"

#Lönehelg/lönevecka?
weekdays_calculator <- read.csv("weekdays_calcul.csv", header = TRUE, sep = ";" )

salarycalc <- function(date,weekday)
{
  
  
}

#you must read in csv as weekdays_calculator
  
  
